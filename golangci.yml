linters:
  enable:
    - unconvert
    - stylecheck
    - lll
    - unparam
    - goimports
    - unparam
    - gofumpt
    - revive
    - unconvert
    - gocognit

  disable:
    - errcheck

linters-settings:
  gocognit:
    min-complexity: 15
  lll:
    line-length: 130
  gofumpt:
    extra-rules: true
